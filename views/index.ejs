<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/styles.css">
  <title>Message Board</title>
</head>
<body>
  <%- include('title') %>
  <%- include('link', {linkText: linkText, route: route}) %>
  <main class="grid-container">
    <% for (let i = 0; i < messages.length; i++) { %>
      <div class="grid-item">
        <h2><%= messages[i].user %></h2>
        <p><%= messages[i].text%></p>
        <a class="details-button" href="/details/<%= i %>">Message Details</a>
      </div>
      <% } %>
  </main>
</body>
</html>